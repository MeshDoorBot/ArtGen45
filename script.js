const STORY_POS={x:128,nowPlayingY:980,dateY:1120};const DJ_LINE_OFFSET=40;const MAX_PILL_WIDTH=1080-256;const PILL_PADDING_X=36;const DATE_TIME_GAP=16;const PILL_VERTICAL_GAP=16;let shows=[];let bgData=null;async function loadShows(){const picker=document.getElementById('showPicker');try{const res=await fetch('https://script.google.com/macros/s/AKfycbw3QgJtZXB48I0BUMpTE5Dlo2kqMReNuD4jbiEVBaB1z49bSgzEkAgixkjHxwTKRwY0/exec');shows=await res.json()}catch(e){shows=[]}picker.innerHTML='<option disabled selected>Select a show</option>';shows.forEach((s,i)=>{const d=new Date(s.Date);const opt=document.createElement('option');opt.value=i;opt.textContent=`${d.toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'short'})} — ${s['Now Playing']}`;picker.appendChild(opt)});picker.disabled=false}document.getElementById('uploadInput').addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=ev=>{bgData=ev.target.result;document.getElementById('renderBtn').disabled=false};r.readAsDataURL(f)});document.getElementById('renderBtn').addEventListener('click',drawStory);function to12h(time){const[h,m]=time.split(':').map(Number);const hr=((h+11)%12)+1;return`${hr}${m?':'+m.toString().padStart(2,'0'):''}${h>=12?'pm':'am'}`}function drawVignette(ctx,w,h){const g=ctx.createRadialGradient(w/2,h/2,Math.min(w,h)*0.25,w/2,h/2,Math.max(w,h)*0.85);g.addColorStop(0,'rgba(0,0,0,0)');g.addColorStop(1,'rgba(0,0,0,0.6)');ctx.fillStyle=g;ctx.fillRect(0,0,w,h)}function drawWhitePill(ctx,x,y,w,h){ctx.fillStyle='#fff';ctx.beginPath();ctx.roundRect(x,y,w,h,16);ctx.fill()}function scaleFontsToFit(ctx,primaryText,secondaryText,maxTextWidth){ctx.font='64px MeshFont';const primaryW=ctx.measureText(primaryText).width;ctx.font='42px MeshFont';const secondaryW=secondaryText?ctx.measureText(secondaryText).width:0;const widest=Math.max(primaryW,secondaryW);if(widest<=maxTextWidth)return{primarySize:64,secondarySize:42};const scale=maxTextWidth/widest;return{primarySize:Math.floor(64*scale),secondarySize:Math.floor(42*scale)}}function drawStory(){const canvas=document.getElementById('c');const ctx=canvas.getContext('2d');const cw=canvas.width;const ch=canvas.height;const show=shows[document.getElementById('showPicker').value];if(!show||!bgData)return;ctx.clearRect(0,0,cw,ch);const bg=new Image();bg.src=bgData;bg.onload=()=>{const scale=Math.max(cw/bg.naturalWidth,ch/bg.naturalHeight);const sw=cw/scale;const sh=ch/scale;ctx.drawImage(bg,(bg.naturalWidth-sw)/2,(bg.naturalHeight-sh)/2,sw,sh,0,0,cw,ch);drawVignette(ctx,cw,ch);const guest=document.getElementById('guestInput').value.trim();const title=show['Now Playing'];const showEqualsDJ=show.DJ&&title.trim().toLowerCase()===show.DJ.trim().toLowerCase();let primaryLine=title;let secondaryLine='';if(showEqualsDJ){if(guest)primaryLine=`${title} + ${guest}`}else{secondaryLine=show.DJ+(guest?' + '+guest:'')}const dateStr=new Date(show.Date).toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'short'});const[startRaw,endRaw]=show['A-B'].split('-');const timeStr=`${to12h(startRaw)} → ${to12h(endRaw)}`;const maxTextW=MAX_PILL_WIDTH-(PILL_PADDING_X*2);const sizes=scaleFontsToFit(ctx,primaryLine,secondaryLine,maxTextW);ctx.font=`${sizes.primarySize}px MeshFont`;const primaryW=ctx.measureText(primaryLine).width;ctx.font=`${sizes.secondarySize}px MeshFont`;const secondaryW=secondaryLine?ctx.measureText(secondaryLine).width:0;let pillW=Math.max(primaryW,secondaryW)+(PILL_PADDING_X*2);let pillX=STORY_POS.x;if(pillW>MAX_PILL_WIDTH){pillW=MAX_PILL_WIDTH;pillX=(cw-pillW)/2}const pillHeight=secondaryLine?140:92;const pillY=STORY_POS.nowPlayingY;drawWhitePill(ctx,pillX,pillY,pillW,pillHeight);const centerY=pillY+(pillHeight/2);const primaryH=sizes.primarySize*0.95;const secondaryH=sizes.secondarySize*0.95;const gap=secondaryLine?Math.max(10,Math.round(sizes.secondarySize*0.35)):0;const blockH=secondaryLine?(primaryH+gap+secondaryH):primaryH;const blockTop=centerY-(blockH/2);const primaryY=blockTop+(primaryH/2);const secondaryY=blockTop+primaryH+gap+(secondaryH/2);ctx.fillStyle='#000';ctx.textBaseline='middle';ctx.textAlign='left';ctx.font=`${sizes.primarySize}px MeshFont`;ctx.fillText(primaryLine,pillX+PILL_PADDING_X,secondaryLine?primaryY:centerY);if(secondaryLine){ctx.globalAlpha=0.75;ctx.font=`${sizes.secondarySize}px MeshFont`;ctx.fillText(secondaryLine,pillX+PILL_PADDING_X,secondaryY);ctx.globalAlpha=1}const dateY=(showEqualsDJ?STORY_POS.dateY-DJ_LINE_OFFSET:STORY_POS.dateY)+PILL_VERTICAL_GAP;ctx.font='32px MeshFont';const dateW=ctx.measureText(dateStr).width+40;const timeW=ctx.measureText(timeStr).width+40;const startX=pillX;drawWhitePill(ctx,startX,dateY,dateW,52);drawWhitePill(ctx,startX+dateW+DATE_TIME_GAP,dateY,timeW,52);ctx.fillStyle='#000';ctx.textBaseline='middle';ctx.textAlign='center';ctx.fillText(dateStr,startX+dateW/2,dateY+26);ctx.fillText(timeStr,startX+dateW+DATE_TIME_GAP+timeW/2,dateY+26);ctx.textAlign='left';ctx.fillStyle='rgba(255,255,255,0.75)';ctx.font='26px MeshFont';ctx.fillText('meshradio.live',startX,dateY+70);document.getElementById('downloadBtn').disabled=false}}document.getElementById('downloadBtn').onclick=async()=>{const blob=await new Promise(r=>document.getElementById('c').toBlob(r,'image/png',1));const filename='mesh_instagram_post.png';const isIOS=/iPhone|iPad|iPod/i.test(navigator.userAgent);if(!isIOS){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);return}const file=new File([blob],filename,{type:'image/png'});if(navigator.canShare&&navigator.canShare({files:[file]})){try{await navigator.share({files:[file]});return}catch(e){}}const url=URL.createObjectURL(blob);window.open(url,'_blank');setTimeout(()=>URL.revokeObjectURL(url),60000)};window.onload=loadShows;